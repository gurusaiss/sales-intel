// Single place to point the extension at your backend. Change this one line
// after deploying (e.g. to "https://sales-intel-backend.onrender.com"), and
// also update host_permissions in manifest.json to match.
const API_BASE = "http://localhost:4000";
