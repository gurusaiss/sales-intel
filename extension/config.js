// Single place to point the extension at your backend.
const API_BASE = "https://sales-intel-backend-ctvc.onrender.com";

// Must match the APP_API_KEY set on the Render backend service. Leave blank
// only if the backend has no APP_API_KEY configured (local dev default).
const API_KEY = "";
